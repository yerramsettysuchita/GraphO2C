# Copy to .env.local for local development.
# Vite exposes only VITE_* variables to the browser bundle.

# Must match GRAPHO2C_API_KEY in backend/.env
# Leave empty if backend has no API key configured.
VITE_API_KEY=your_grapho2c_api_key_here
